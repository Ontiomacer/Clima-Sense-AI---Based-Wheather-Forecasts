# GraphCast Forecast Components Integration Guide

This guide shows how to integrate the `ForecastMetricsTable` and `GraphCastHeatmap` components together in the agriculture page.

## Components Overview

1. **GraphCastHeatmap**: Visual heatmap overlay showing risk metrics on a map
2. **ForecastMetricsTable**: Sortable table with expandable rows and CSV export

## Integration Pattern

Both components share the same f